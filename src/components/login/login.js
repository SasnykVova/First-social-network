import s from './login.module.scss';

const Login = (props) => {
    return (
        <div class={s.login}>
            <h1 class={s.login__title}>Login</h1>
        </div>
    )
}

export default Login;